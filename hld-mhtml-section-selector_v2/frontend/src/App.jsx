import {useState} from "react";import {extractSections} from "./services/api";import SectionSelector from "./components/SectionSelector";
export default function App(){const[result,setResult]=useState(null),[selected,setSelected]=useState(new Set()),[busy,setBusy]=useState(false),[error,setError]=useState("");async function upload(file){if(!file)return;setBusy(true);setError("");try{setResult(await extractSections(file));setSelected(new Set())}catch(e){setError(e.message)}finally{setBusy(false)}}return <main><div className="brand"><div>HLD</div><section><h1>HLD Review Assistant</h1><p>Extract validated sections from an MHTML design document.</p></section></div>{error&&<div className="error">{error}</div>}{busy?<div className="loading">Reading and filtering the HLD...</div>:result?<SectionSelector result={result} selected={selected} onChange={setSelected} onReset={()=>setResult(null)}/>:<label className="drop"><strong>Upload HLD document</strong><span>Choose a .mhtml or .mht file</span><input type="file" accept=".mhtml,.mht" onChange={e=>upload(e.target.files?.[0])}/></label>}</main>}
